// 15. Crear la interface de tipo enum, por defecto da valor 0,1,2, cambiar al mismo valor de nombre porque si se mueve algo se afecta la numeracion
export enum AuthStatus{
  checking ='checking',
  authenticated = 'authenticated',
  notAuthenticated = 'notAuthenticated'
}
